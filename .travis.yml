stages:
        - build_debian-ncp
        - build_lamp
        - build_nc
        - build_ncp
sudo: required
cache:
  directories:
        - /var/lib/docker
language: generic
addons:
  apt:
    packages:
        - docker-ce #Upgrade Docker to 18.06 version that supports experimental features
services:
        - docker
branches:
  only:
        - gsoc2019-travis
cache:
  directories:
        - /var/lib/docker
before_script:
        - ./.travis/main.sh #configuration script for travis machine
jobs:
  include:
          #        - stage: configuration
          #          script: ./.travis/main.sh #configuration script for travis machine

        - stage: build_debian-ncp
          script: while sleep 9m; do echo "=====[ $SECONDS seconds, build-docker still building... ]====="; done & ./.travis/build-docker_test.sh x86 > output
        -
          script: while sleep 9m; do echo "=====[ $SECONDS seconds, build-docker still building... ]====="; done & ./.travis/build-docker_test.sh armhf > output
        -
          script: while sleep 9m; do echo "=====[ $SECONDS seconds, build-docker still building... ]====="; done & ./.travis/build-docker_test.sh arm64 > output

        - stage: build_lamp
          script: ls #Will fix later

        - stage: build_nc
          script: ls #Will fix later

        - stage: build_ncp
          script: ls #Will fix later
